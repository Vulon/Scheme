#lang racket

(define (single_sort inp_list id)
  (cond
    [(= id (- (length inp_list) 1)) inp_list]
    [(> (list-ref inp_list id) (list-ref inp_list (+ id 1)) )
     (append () () )]
    [else inp_list]
    )
  )